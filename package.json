{
  "name": "root",
  "private": true,
  "description": "Training material on testing FE & BE apps",
  "license": "MIT",
  "author": "Andrew Moss <andrew@m0ss.dev> (https://m0ss.dev/)",
  "workspaces": {
    "packages": ["packages/*"],
    "nohoist": ["**/@nestjs", "**/@nestjs/**"]
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "bootstrap:clean": "lerna clean -y && lerna bootstrap --use-workspaces",
    "test": "lerna run test --stream --parallel"
  },
  "devDependencies": {
    "lerna": "^5.5.0"
  },
  "engines": {
    "node": "16.X",
    "yarn": "1.X"
  }
}